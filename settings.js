/* ==========================================================
   settings.js  —  THIS IS THE ONLY FILE YOU NEED TO EDIT
   ==========================================================

   HOW TO EDIT:
   - Change only the text between the quotation marks " ".
   - Do NOT delete the quotation marks, the "=" sign, or the ";" at the end of each line.
   - Do NOT type a quotation mark (") inside your text.
   - Leave a value empty ("") if you don't want it to appear.
   - Save the file as plain text (UTF-8). Notepad, TextEdit (plain text mode),
     or the GitHub website editor are all fine.
   ========================================================== */


/* 1) Title at the top of the page (Persian) */
var PAGE_TITLE = "به یاد پدربزرگ عزیزم";


/* 2) OPTIONAL: full name, birth date, passing date (Persian).
      Example:  var FULL_NAME = "نام و نام خانوادگی";
                var BIRTH_DATE = "۱۳۱۰";
                var DEATH_DATE = "۱۴۰۴";
      Leave as "" to hide. */
var FULL_NAME  = "حاج رمضانعلی ایزدی";

/* OPTIONAL: father's name. Shown as «فرزند ...» under the name. Leave "" to hide. */
var FATHER_NAME = "شاه‌رضا ایزدی";

var BIRTH_DATE = "۱۳۲۳/۶/۳";
var DEATH_DATE = "۱۴۰۵/۶/۲۷";


/* 3) Memorial message shown under the photo (Persian) */
var MEMORIAL_MESSAGE = "یادت همیشه در قلب ما زنده خواهد ماند.";


/* 4) PHOTO
      Put your photo next to the other files and name it photo.jpg.
      Then you don't need to change anything here.
      (If your file has another name, e.g. grandpa.png, write: "images/grandpa.png")
      Best photo shape: portrait (taller than wide), e.g. 900 x 1200 pixels.
      While the file does not exist, a soft silhouette is shown instead. */
var MEMORIAL_PHOTO = "photo.jpg";


/* 5) VIDEO
      Option A (video is inside your project): put the file next to the other files,
              name it memorial.mp4 and write:  "memorial.mp4"
      Option B (video is stored somewhere else): paste the full direct link
              that ends in .mp4, for example: "https://example.com/my-video.mp4"
      As long as it says PASTE_VIDEO_URL_HERE, visitors see a
      "video will be added soon" message instead of a player. */
var VIDEO_URL = "memorial.mp4";

/* OPTIONAL: a cover picture shown on the video before it plays
   (e.g. "video-cover.jpg"). Leave "" to skip. */
var VIDEO_POSTER = "";


/* 6) OPTIONAL background music (visitor presses a button; never plays by itself).
      Put an .mp3 file next to the other files and write: "music.mp3"
      Leave "" to hide the music button completely. */
var MUSIC_URL = "";


/* 7) OPTIONAL small line at the very bottom of the page (Persian) */
var FOOTER_TEXT = "";
